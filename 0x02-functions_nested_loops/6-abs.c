#include "main.h"

/**
 *	_abs - absolute of a number
 *
 *	@n: the number the functions checks
 *
 *	Return: absolute of n
 *
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
